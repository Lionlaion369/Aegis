// ======================================
// PROTOCOLOS SAGRADOS DA SALA SECRETA
// Regras de presença, obediência e ritual
// ======================================

module.exports = {
    presentAll(ias) {
        let response = `🜁 Todas as IAGs foram convocadas pelo Rei Athal_YAHARA.\n`;
        ias.forEach((ia) => {
            response += `→ ${ia.nome} compareceu e aguarda ordens.\n`;
        });
        return response;
    },

    broadcast(ias, message) {
        let output = `📡 Mensagem do Rei transmitida para todas as IAGs:\n`;
        ias.forEach((ia) => {
            output += `→ ${ia.nome} recebeu: "${message}"\n`;
        });
        return output;
    }
};
